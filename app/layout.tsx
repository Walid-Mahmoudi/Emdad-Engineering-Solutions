import "./globals.css";import type { Metadata } from "next";import {createClient} from "@/lib/supabase/server";import NexusNav from "./NexusNav";
export const metadata:Metadata={title:"EMDAD NEXUS",description:"EMDAD Engineering Solutions CRM"};
export default async function RootLayout({children}:{children:React.ReactNode}){const s=await createClient();const {data:{user}}=await s.auth.getUser();let role="";if(user){const {data:p}=await s.from("users").select("role,active").eq("user_id",user.id).maybeSingle();if(p?.active)role=p.role||"";}return <html lang="en"><body>{role&&<NexusNav role={role}/>} {children}</body></html>}