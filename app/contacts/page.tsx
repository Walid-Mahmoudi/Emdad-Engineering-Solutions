import { createClient } from "@/lib/supabase/server";
import ContactsClient from "./ContactsClient";
import { UsersRound, UserPlus } from "lucide-react";
export default async function ContactsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main className="nexus-page"><h1>Contacts</h1><p>Unauthorized</p></main>;const {data}=await supabase.from("contacts").select("*").order("updated_at",{ascending:false});return <main className="nexus-page contacts-workspace"><header className="nexus-page-head"><div><div className="eyebrow">CUSTOMER MANAGEMENT</div><h1>Contacts</h1><p>People connected to client companies, projects and buying roles.</p></div><div className="nexus-head-actions"><span className="workspace-chip"><UsersRound size={14}/> {data?.length||0} contacts</span></div></header><ContactsClient contacts={data??[]}/></main>}