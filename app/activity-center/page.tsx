import { createClient } from "@/lib/supabase/server";
export default async function ActivityPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="nexus-page"><h1>Activity Center</h1><p>Unauthorized</p></main>;const {data}=await s.from("audit_log").select("log_id,timestamp,user,action,entity_type,entity_id,details").order("timestamp",{ascending:false}).limit(100);return <main className="nexus-page"><div className="nexus-header"><div><h1>Activity Center</h1><p>Recent stage, follow-up, contract and collection activity.</p></div></div><section className="nexus-card"><div className="nexus-table-wrap"><table className="nexus-table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th><th>ID</th><th>Details</th></tr></thead><tbody>{(data||[]).map(x=><tr key={x.log_id}><td>{x.timestamp}</td><td>{x.user||"—"}</td><td>{x.action}</td><td>{x.entity_type}</td><td>{x.entity_id||"—"}</td><td>{x.details||"—"}</td></tr>)}{!data?.length&&<tr><td colSpan={6}>No activity yet.</td></tr>}</tbody></table></div></section></main>}