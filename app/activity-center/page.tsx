import { createClient } from "@/lib/supabase/server";
import { Activity, Clock3, UserRound, ArrowUpRight } from "lucide-react";
export default async function ActivityPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="nexus-page"><h1>Activity Center</h1><p>Unauthorized</p></main>;const {data}=await s.from("audit_log").select("log_id,timestamp,user_email,action,entity_type,entity_id,details").order("timestamp",{ascending:false}).limit(100);return <main className="nexus-page activity-workspace"><header className="nexus-page-head"><div><div className="eyebrow">CRM AUDIT & ACTIVITY</div><h1>Activity Center</h1><p>One chronological view of changes across projects, deals and customer activity.</p></div><div className="nexus-head-actions"><span className="workspace-chip"><Activity size={14}/> Live audit trail</span></div></header><section className="activity-summary"><div><Activity size={16}/><span>Total events</span><strong>{data?.length||0}</strong></div><div><Clock3 size={16}/><span>Latest activity</span><strong>{data?.[0]?new Date(data[0].timestamp).toLocaleDateString("en-GB",{day:"2-digit",month:"short"}):"—"}</strong></div><div><UserRound size={16}/><span>Users active</span><strong>{new Set((data||[]).map(x=>x.user_email).filter(Boolean)).size}</strong></div></section><section className="nexus-card"><div className="section-head"><div><h2>Recent activity</h2><p className="muted">Latest 100 audit events.</p></div><ArrowUpRight size={17}/></div><div className="nexus-table-wrap"><table className="nexus-table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th><th>ID</th><th>Details</th></tr></thead><tbody>{(data||[]).map(x=><tr key={x.log_id}><td>{x.timestamp}</td><td>{x.user_email||"—"}</td><td>{x.action}</td><td>{x.entity_type}</td><td>{x.entity_id||"—"}</td><td>{x.details||"—"}</td></tr>)}{!data?.length&&<tr><td colSpan={6}>No activity yet.</td></tr>}</tbody></table></div></section></main>}