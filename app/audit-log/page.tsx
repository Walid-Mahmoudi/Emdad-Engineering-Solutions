import { createClient } from "@/lib/supabase/server";
export default async function AuditPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="nexus-page"><h1>Audit Log</h1><p>Unauthorized</p></main>;const {data}=await s.from("audit_log").select("log_id,timestamp,user,action,entity_type,entity_id,details").order("timestamp",{ascending:false}).limit(200);return <main className="nexus-page"><div className="nexus-header"><div><h1>Audit Log</h1><p>Traceable system operations.</p></div></div><section className="nexus-card"><div className="nexus-table-wrap"><table className="nexus-table"><thead><tr><th>Timestamp</th><th>User</th><th>Action</th><th>Entity</th><th>Entity ID</th><th>Details</th></tr></thead><tbody>{(data||[]).map(x=><tr key={x.log_id}><td>{x.timestamp}</td><td>{x.user||"—"}</td><td>{x.action}</td><td>{x.entity_type}</td><td>{x.entity_id||"—"}</td><td>{x.details||"—"}</td></tr>)}</tbody></table></div></section></main>}