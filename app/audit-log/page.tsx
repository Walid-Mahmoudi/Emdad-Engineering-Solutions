import { createClient } from "@/lib/supabase/server";
import { History, ShieldCheck } from "lucide-react";
export default async function AuditPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="nexus-page"><h1>Audit Log</h1><p>Unauthorized</p></main>;const {data:profile}=await s.from("users").select("role,active").eq("user_id",user.id).maybeSingle();if(!profile?.active || !["Admin","Manager"].includes(profile.role))return <main className="nexus-page"><h1>Audit Log</h1><p>Access restricted to Admin and Manager.</p></main>;const {data}=await s.from("audit_log").select("log_id,timestamp,user_email,action,entity_type,entity_id,details").order("timestamp",{ascending:false}).limit(200);const rows=data??[];return <main className="nexus-page audit-workspace"><header className="nexus-page-head"><div><div className="eyebrow">ADMINISTRATION</div><h1>Audit Log</h1><p>Traceable CRM operations and system history.</p></div><div className="nexus-head-actions"><span className="workspace-chip"><ShieldCheck size={14}/> {rows.length} events loaded</span></div></header><section className="dashboard-stat-grid audit-kpis"><div className="nexus-stat-card"><div className="nexus-stat-icon blue"><History size={18}/></div><div><span>Events</span><strong>{rows.length}</strong><small>Latest 200 records</small></div></div><div className="nexus-stat-card"><div className="nexus-stat-icon purple"><ShieldCheck size={18}/></div><div><span>Audit Scope</span><strong>CRM</strong><small>Projects · Activities · Finance</small></div></div></section><section className="nexus-card"><div className="nexus-table-wrap"><table className="nexus-table"><thead><tr><th>Timestamp</th><th>User</th><th>Action</th><th>Entity</th><th>Entity ID</th><th>Details</th></tr></thead><tbody>{rows.map(x=><tr key={x.log_id}><td>{x.timestamp}</td><td>{x.user_email||"—"}</td><td>{x.action}</td><td>{x.entity_type}</td><td>{x.entity_id||"—"}</td><td>{x.details||"—"}</td></tr>)}{!rows.length&&<tr><td colSpan={6}><div className="nexus-empty-inline">No audit events yet.</div></td></tr>}</tbody></table></div></section></main>}