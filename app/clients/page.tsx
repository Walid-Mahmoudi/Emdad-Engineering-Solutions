import { createClient } from "@/lib/supabase/server";import Link from "next/link";
export default async function ClientsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return <main className="nexus-page"><h1>Clients</h1><p>Unauthorized</p></main>;const {data}=await supabase.from("projects").select("project_id,project_name,client,current_action,estimated_value");const map=new Map<string,any>();for(const p of data||[]){const n=p.client||"No Client";const x=map.get(n)||{name:n,projects:0,active:0,focus:0,pipeline:0};x.projects++;if(!["Closed Won","Closed Lost"].includes(p.current_action)){x.active++;x.pipeline+=Number(p.estimated_value||0)}if(["Tender – High Probability","In Hand","Negotiation"].includes(p.current_action))x.focus++;map.set(n,x)}const rows=Array.from(map.values()).sort((a,b)=>b.pipeline-a.pipeline);return <main className="nexus-page"><div className="nexus-header"><div><h1>Clients</h1><p>Client 360 overview.</p></div></div><section className="nexus-card"><div className="nexus-table-wrap"><table className="nexus-table"><thead><tr><th>Client</th><th>Projects</th><th>Active</th><th>Focus</th><th>Pipeline Value</th></tr></thead><tbody>{rows.map(x=><tr key={x.name}><td>{x.name}</td><td>{x.projects}</td><td>{x.active}</td><td>{x.focus}</td><td>{x.pipeline.toLocaleString()} EGP</td></tr>)}{!rows.length&&<tr><td colSpan={5}>No clients found.</td></tr>}</tbody></table></div></section></main>}