import { createClient } from "@/lib/supabase/server";
import DataManagementClient from "./DataManagementClient";
export default async function DataManagementPage(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <main className="nexus-page"><h1>Data Management</h1><p>Unauthorized</p></main>;const {data:profile}=await s.from("users").select("role").eq("user_id",user.id).maybeSingle();if(!profile||!["Admin","Manager"].includes(profile.role))return <main className="nexus-page"><h1>Data Management</h1><p>Access restricted to Admin and Manager.</p></main>;return <main className="nexus-page"><div className="nexus-header"><div><h1>Data Management</h1><p>Controlled data operations for EMDAD NEXUS.</p></div></div><DataManagementClient/></main>}