"use client";
import {useState} from "react";import {getBackup,runQualityScan} from "./actions";
export default function DataManagementClient(){const [busy,setBusy]=useState("");const [result,setResult]=useState<any>(null);
async function backup(){setBusy("backup");try{const d=await getBackup();const blob=new Blob([JSON.stringify(d,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="EMDAD-NEXUS-backup.json";a.click();URL.revokeObjectURL(a.href)}catch(e:any){setResult({error:e.message})}finally{setBusy("")}}
async function scan(){setBusy("scan");try{setResult(await runQualityScan())}catch(e:any){setResult({error:e.message})}finally{setBusy("")}}
return <div className="grid"><section className="nexus-card"><h2>Backup</h2><p>Export a full JSON snapshot of the CRM tables.</p><button className="button" onClick={backup} disabled={!!busy}>{busy==="backup"?"Preparing…":"Download Full JSON Backup"}</button></section><section className="nexus-card"><h2>Data Quality</h2><p>Scan projects for missing or invalid core data.</p><button className="button secondary" onClick={scan} disabled={!!busy}>{busy==="scan"?"Scanning…":"Run Scan"}</button></section>{result&&<section className="nexus-card" style={{gridColumn:"1/-1"}}><h2>Scan Result</h2><pre style={{whiteSpace:"pre-wrap"}}>{JSON.stringify(result,null,2)}</pre></section>}</div>}
