"use server";
import { createClient } from "@/lib/supabase/server";
async function admin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)throw new Error("Unauthorized");const {data:p}=await s.from("users").select("role").eq("user_id",user.id).maybeSingle();if(!p||!["Admin","Manager"].includes(p.role))throw new Error("Access denied");return s}
export async function getBackup(){const s=await admin();const tables=["projects","action_history","follow_ups","contracts","collections","users","settings","deleted_projects","audit_log","contacts","attachments","notifications","automation_log"];const out:any={exportedAt:new Date().toISOString()};for(const t of tables){const {data,error}=await s.from(t).select("*");if(error)throw new Error(t+": "+error.message);out[t]=data||[]}return out}
export async function runQualityScan(){const s=await admin();const findings:any[]=[];const {data:p}=await s.from("projects").select("project_id,project_name,client,current_action,estimated_value,next_followup_date");for(const x of p||[]){if(!x.project_name)findings.push({severity:"Critical",type:"Missing project name",project_id:x.project_id});if(x.estimated_value!=null&&Number(x.estimated_value)<0)findings.push({severity:"Critical",type:"Negative estimated value",project_id:x.project_id});if(!x.current_action)findings.push({severity:"Critical",type:"Missing stage",project_id:x.project_id});if(!x.client)findings.push({severity:"Warning",type:"Missing client",project_id:x.project_id});if(!["Tender","Tender – High Probability","In Hand","Negotiation","Closed Won","Closed Lost"].includes(x.current_action||""))findings.push({severity:"Critical",type:"Invalid pipeline stage",project_id:x.project_id})}return {scanned:p?.length||0,findings}}
